// Temporarily disabled Zyzio until native WebGPU part rendering is implemented
import { WebPhysics } from '../webPhysics';

export class Zyzio {
    constructor(physics: WebPhysics, spawnPos: any) {}
    update(mouseWorld: any) {}
    dispose() {}
}